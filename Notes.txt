npm init -y:
initializing the project directory

installation of only the essential tools for running and executable of typescript backend
npm install typescript @types/node tsx express @types/jsonwebtoken dotenv cors @types/cors -D 

initializing typescript config file
npx tsc --init

Creating the required directory structure
New-Item -ItemType Directory -Path src\models, src\middleware, src\services, src\controllers
